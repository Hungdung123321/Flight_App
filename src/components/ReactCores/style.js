import { StyleSheet } from "react-native";
import { TITLE_FONT } from "../../assets/fonts";

const styles = StyleSheet.create({
    SnesTitle: {
        fontFamily: TITLE_FONT.SNES_ITALIC,
    }
})

export default styles;